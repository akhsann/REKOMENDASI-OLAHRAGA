import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rekomendasi.olahraga',
  appName: 'Rekomendasi Olahraga',
  webDir: 'dist'
};

export default config;
