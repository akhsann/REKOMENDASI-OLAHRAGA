import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rekomendasi.olahraga',
  appName: 'com.rekomendasi.olahraga',
  webDir: 'dist'
};

export default config;
